node_modules/
dist/
build/
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Private backend/server environment files must never be committed.
backend/.env
backend/.env.*
**/backend/.env
**/backend/.env.*

# Local Vite overrides stay private.
frontend/.env.local
frontend/.env.*.local
admin/.env.local
admin/.env.*.local

# These two contain only the public API origin and are intentionally versioned.
!frontend/.env
!admin/.env
