# Public browser configuration only. This is safe to commit.
VITE_API_MODE=hosted
VITE_HOSTED_BACKEND_URL=https://soundwave-music.onrender.com

# Optional: only set this when intentionally developing against a local API.
# VITE_API_MODE=local
# VITE_LOCAL_BACKEND_URL=http://localhost:4000
